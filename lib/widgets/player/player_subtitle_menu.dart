import 'package:flutter/material.dart';

/// Placeholder for subtitle track selection (Phase 3 refinement).
class PlayerSubtitleMenu extends StatelessWidget {
  const PlayerSubtitleMenu({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
